// import { useState } from 'react';

// const useBookOrder = () => {
//   const [bookOrder, setBookOrder] = useState(shuffle(Object.keys(books)));

//   const shuffleBookOrder = () => {
//     setBookOrder(shuffle(bookOrder));
//   };

//   return [bookOrder, shuffleBookOrder];
// };

// export default useBookOrder;
